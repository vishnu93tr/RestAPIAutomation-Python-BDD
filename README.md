# RestAPIAutomation-Python-BDD

rest API automation using python behave 

steps to run:

behave feature/<feature-file-to-run> --no-color --no-capture


Allure report generation:
allure serve <path to json files>
